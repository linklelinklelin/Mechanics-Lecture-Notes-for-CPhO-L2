#import "../../format/lib.typ":*
#import "../../format/utils.typ":*

== 纯滚动

纯滚动是刚体平面运动的一种特殊形式，常见于圆轮在轨道上的运动 . 其核心特征是刚体与接触面之间无相对滑动 . 

=== 纯滚动的定义与几何约束

当刚体在固定曲面（或平面）上运动时，若接触点无相对滑动，则称为*纯滚动* . 此时，质心运动与绕质心的转动满足以下几何约束：

$ v_C = omega R, quad a_C = alpha R $

其中：
- $v_C$ 为质心速度，$a_C$ 为质心切向加速度；

- $omega$ 为刚体转动角速度，$alpha$（或 $beta$）为角加速度；
- $R$ 为转动/滚动半径 . 

=== 瞬时速度中心（瞬心）

1. *定义*：在平面运动中，刚体上瞬时速度为零的点称为*瞬心* . 

2. *纯滚动的瞬心*：纯滚动时，刚体与地面的*接触点* $P$ 即为瞬心，即 $arrow(bold(v))_P = 0$ . 
3. *速度分布*：刚体上任意点 $Q$ 的速度可以表示为绕瞬心 $P$ 的瞬时转动：

$ arrow(bold(v))_Q = arrow(bold(omega)) times arrow(bold(P Q)) $

   - *运动分解*：纯滚动可分解为随质心的平动（速度 $v_C$）和绕质心的转动（角速度 $omega$） . 但在速度分析中，统一视为绕瞬心 $P$ 的瞬时转动往往更为简便 . 

=== 加速度特性

需要特别注意，虽然瞬心 $P$ 的瞬时速度为零，但其*加速度不为零* . 

1. *瞬心的加速度*：

$ arrow(bold(a))_P = omega^2 R (bold(hat(n))) $
   其中 $arrow(bold(n))$ 为指向质心的单位矢量 . 这表明瞬心在此时刻只有法向加速度 . 
   
2. *质心的加速度*：

$ arrow(bold(a))_C = (alpha R) (bold(hat(tau))) + (omega^2 R) (bold(hat(n))) $
   其中 $(bold(hat(tau)))$ 指向质心运动的切向，$(bold(hat(n)))$ 指向接触面法向 . 对于在平直轨道上滚动的圆轮，$arrow(bold(a))_C$ 的大小通常简化为 $alpha R$ . 

#pagebreak()

#example(title:"滚线1")[
  
  一个刚性圆轮在直线轨道上作纯滚动，圆轮边缘上一点所经历的轨迹称为滚线（又称旋轮线、摆线） . 所谓纯滚动就是圆轮与直线轨道的接触点无相对运动 . 设圆轮半径为 $R$ . 
  
  (1) 试写出滚线的轨道方程（利用滚过的角度 $phi$ 作为参量）；

  (2) 试求滚线上各点的曲率半径 . 

  #align(right)[
   #block(width: 70%)[  // 限制 figure 的容器宽度
    #figure(
      image("image/滚线.png", width: 100%),
      //caption: [滚线、摆线、旋轮线]
    )
  ]
  ]


]